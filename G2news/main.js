// import { fetchData } from './Scripts/fetch.js';
import { getArticle } from "./Scripts/scraping";
// fetchData();
await getArticle();


//const input = input;
//const initial = `science`
//const categoryChoice = `category=${input}`

/* function getarticles(category){
    axios(url/category=${category})
} */

//getarticles(initla);

//getarticles(categoryChoice);

/*
if(query === empty){
    axios(url)
} 
else{
    axios(url_category)
}
 */